name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

env:
  STOOLAP_ENGINE_REF: v0.4.0

jobs:
  build:
    strategy:
      fail-fast: true
      matrix:
        settings:
          - host: macos-latest
            target: aarch64-apple-darwin
            lib_file: libstoolap.dylib
          - host: macos-latest
            target: x86_64-apple-darwin
            lib_file: libstoolap.dylib
          - host: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            lib_file: libstoolap.so
          - host: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            lib_file: libstoolap.so
          - host: windows-latest
            target: x86_64-pc-windows-gnu
            lib_file: stoolap.dll

    name: Build - ${{ matrix.settings.target }}
    runs-on: ${{ matrix.settings.host }}
    steps:
      - name: Clone stoolap engine
        run: git clone --depth 1 --branch ${{ env.STOOLAP_ENGINE_REF }} https://github.com/stoolap/stoolap.git stoolap-engine

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.settings.target }}

      - name: Add MinGW to PATH
        if: runner.os == 'Windows'
        shell: bash
        run: echo "C:/msys64/mingw64/bin" >> $GITHUB_PATH

      - name: Install cross-compilation tools
        if: matrix.settings.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: stoolap-engine
          key: ${{ matrix.settings.target }}

      - name: Build stoolap shared library
        working-directory: stoolap-engine
        run: cargo build --release --features ffi --target ${{ matrix.settings.target }}
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Fix macOS install name
        if: runner.os == 'macOS'
        run: install_name_tool -id @rpath/libstoolap.dylib stoolap-engine/target/${{ matrix.settings.target }}/release/${{ matrix.settings.lib_file }}

      - name: Strip shared library (macOS)
        if: runner.os == 'macOS'
        run: strip -x stoolap-engine/target/${{ matrix.settings.target }}/release/${{ matrix.settings.lib_file }}

      - name: Strip shared library (Linux/Windows)
        if: runner.os != 'macOS'
        shell: bash
        run: |
          if [[ "${{ matrix.settings.target }}" == "aarch64-unknown-linux-gnu" ]]; then
            aarch64-linux-gnu-strip stoolap-engine/target/${{ matrix.settings.target }}/release/${{ matrix.settings.lib_file }}
          else
            strip stoolap-engine/target/${{ matrix.settings.target }}/release/${{ matrix.settings.lib_file }}
          fi

      - name: Upload shared library
        uses: actions/upload-artifact@v4
        with:
          name: lib-${{ matrix.settings.target }}
          path: stoolap-engine/target/${{ matrix.settings.target }}/release/${{ matrix.settings.lib_file }}
          if-no-files-found: error

  build-wasm:
    name: Build - wasm32-wasip1
    runs-on: ubuntu-latest
    steps:
      - name: Clone stoolap engine
        run: git clone --depth 1 --branch ${{ env.STOOLAP_ENGINE_REF }} https://github.com/stoolap/stoolap.git stoolap-engine

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip1

      - name: Install wasm-opt
        run: |
          curl -sL https://github.com/WebAssembly/binaryen/releases/download/version_121/binaryen-version_121-x86_64-linux.tar.gz | tar xz
          echo "$PWD/binaryen-version_121/bin" >> $GITHUB_PATH

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: stoolap-engine
          key: wasm32-wasip1

      - name: Build stoolap WASM
        working-directory: stoolap-engine
        run: cargo build --profile max --target wasm32-wasip1 --features ffi --no-default-features

      - name: Optimize WASM
        run: wasm-opt -Oz stoolap-engine/target/wasm32-wasip1/max/stoolap.wasm -o stoolap.wasm

      - name: Show size
        run: ls -lh stoolap.wasm

      - name: Upload WASM
        uses: actions/upload-artifact@v4
        with:
          name: wasm-stoolap
          path: stoolap.wasm
          if-no-files-found: error

  test:
    name: Test CGO - go${{ matrix.go }} (${{ matrix.settings.os }})
    needs: build
    strategy:
      fail-fast: true
      matrix:
        settings:
          - os: macos-latest
            artifact: lib-aarch64-apple-darwin
            lib_dir: lib/darwin_arm64
          - os: ubuntu-latest
            artifact: lib-x86_64-unknown-linux-gnu
            lib_dir: lib/linux_amd64
          - os: windows-latest
            artifact: lib-x86_64-pc-windows-gnu
            lib_dir: lib/windows_amd64
        go: ['1.24', '1.25', '1.26']
    runs-on: ${{ matrix.settings.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: Download shared library
        uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.settings.artifact }}
          path: ${{ matrix.settings.lib_dir }}

      - name: Run tests
        shell: bash
        run: |
          export PATH="${{ github.workspace }}/${{ matrix.settings.lib_dir }}:$PATH"
          export LD_LIBRARY_PATH="${{ github.workspace }}/${{ matrix.settings.lib_dir }}"
          go vet ./...
          go test -v -count=1 ./...
        env:
          CGO_ENABLED: '1'

  test-wasm:
    name: Test WASM - go${{ matrix.go }}
    needs: build-wasm
    strategy:
      fail-fast: true
      matrix:
        go: ['1.24', '1.25', '1.26']
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: Download WASM
        uses: actions/download-artifact@v4
        with:
          name: wasm-stoolap
          path: wasm

      - name: Run WASM driver tests
        working-directory: wasm
        run: |
          go vet ./...
          go test -v -count=1 -cover ./...
        env:
          STOOLAP_WASM: stoolap.wasm
          CGO_ENABLED: '0'

  release:
    name: Commit, Tag & Release
    needs: [test, test-wasm, build, build-wasm]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Extract version
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Download darwin arm64
        uses: actions/download-artifact@v4
        with:
          name: lib-aarch64-apple-darwin
          path: lib/darwin_arm64

      - name: Download linux amd64
        uses: actions/download-artifact@v4
        with:
          name: lib-x86_64-unknown-linux-gnu
          path: lib/linux_amd64

      - name: Download windows amd64
        uses: actions/download-artifact@v4
        with:
          name: lib-x86_64-pc-windows-gnu
          path: lib/windows_amd64

      - name: Download WASM
        uses: actions/download-artifact@v4
        with:
          name: wasm-stoolap
          path: wasm

      - name: Update go.mod versions
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          sed -i "s|lib/darwin_arm64 v[0-9.]*|lib/darwin_arm64 v${VERSION}|" go.mod
          sed -i "s|lib/linux_amd64 v[0-9.]*|lib/linux_amd64 v${VERSION}|" go.mod
          sed -i "s|lib/windows_amd64 v[0-9.]*|lib/windows_amd64 v${VERSION}|" go.mod
          sed -i "s|stoolap-go v[0-9.]*|stoolap-go v${VERSION}|" example/benchmark/go.mod
          sed -i "s|lib/darwin_arm64 v[0-9.]*|lib/darwin_arm64 v${VERSION}|" example/benchmark/go.mod
          sed -i "s|lib/linux_amd64 v[0-9.]*|lib/linux_amd64 v${VERSION}|" example/benchmark/go.mod
          sed -i "s|lib/windows_amd64 v[0-9.]*|lib/windows_amd64 v${VERSION}|" example/benchmark/go.mod

      - name: Commit libraries and WASM
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add lib/ wasm/stoolap.wasm go.mod example/benchmark/go.mod
          git commit -m "Release v${{ steps.version.outputs.VERSION }} (stoolap ${{ env.STOOLAP_ENGINE_REF }})"
          git push origin main

      - name: Move tag and create submodule tags
        run: |
          VERSION="v${{ steps.version.outputs.VERSION }}"
          # Move the main tag to include the libraries
          git tag -f "${VERSION}"
          git push origin "${VERSION}" --force
          # Create lib submodule tags
          git tag "lib/darwin_arm64/${VERSION}"
          git tag "lib/linux_amd64/${VERSION}"
          git tag "lib/windows_amd64/${VERSION}"
          # Create wasm submodule tag
          git tag "wasm/${VERSION}"
          git push origin "lib/darwin_arm64/${VERSION}" "lib/linux_amd64/${VERSION}" "lib/windows_amd64/${VERSION}" "wasm/${VERSION}"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release

          declare -A NAMES=(
            ["aarch64-apple-darwin"]="libstoolap-darwin-arm64.dylib"
            ["x86_64-apple-darwin"]="libstoolap-darwin-x64.dylib"
            ["x86_64-unknown-linux-gnu"]="libstoolap-linux-x64.so"
            ["aarch64-unknown-linux-gnu"]="libstoolap-linux-arm64.so"
            ["x86_64-pc-windows-gnu"]="stoolap-windows-x64.dll"
          )

          for target in "${!NAMES[@]}"; do
            src=$(ls artifacts/lib-${target}/*)
            dest="release/${NAMES[$target]}"
            cp "$src" "$dest"
          done

          # WASM binary
          cp artifacts/wasm-stoolap/stoolap.wasm release/stoolap.wasm

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.VERSION }}
          generate_release_notes: true
          files: release/*
